<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>게시판</title>
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js" integrity="sha256-/xUj+3OJU5yExlq6GSYGSHk7tPXikynS7ogEvDej/m4=" crossorigin="anonymous"></script>
    <script src="https://kit.fontawesome.com/8132c83a04.js" crossorigin="anonymous"></script>
    <!-- modal -->
    <link href="//cdn.jsdelivr.net/npm/@sweetalert2/theme-material-ui/material-ui.css" rel="stylesheet">
    <script src="//cdn.jsdelivr.net/npm/sweetalert2@11/dist/sweetalert2.min.js"></script>
    <!-- css -->
    <link rel="stylesheet" href="/public/css/board.css" /> 
</head>
<body>
    <div class="container">

        <div class="mypage-head"> 😋 SWITH에 오신걸 환영합니다! 😋 //여기는 스터디네임 불러오기
            <button type="button" onclick="goHome()" class="homeBtn">
                <i class="fa-solid fa-house"></i>
            </button>
        </div>

        <div class="modify-list">
                <form id="modify_info">
                    <div class="info-field">
                        <table border="1" cellspacing="0" style="text-align: center;">
                            <tr>
                                <td style="width: 100px;">작성자</td>
                                <td style="width: 300px;">제목</td>
                                <td style="width: 300px;">등록일</td>
                            </tr>
                        </table>
                    </div>
                </form>
            
        </div>
    </div>

    <script>

        // 홈버튼
        function goHome() {
            document.location.href="http://localhost:8000/swith";
            }

        // 스터디 게시물 등록 
        function add_Posting() {
            var tb=document.getElementById('table');
            var tr=tb.insertRow();
            var td1=tr.insertCell();
            var td2=tr.insertCell();
            var td3=tr.insertCell();
            var td4=tr.insertCell();
            var rows=document.getElementsByTagName('tr').length;

            var user_nickname=document.getElementById('board_user_name').value;
            var user_content=document.getElementById('board_content').value;
            var date=new Date();
            var number=rows-1;

            td1.innerHTML=number;
            td2.innerHTML=user_nickname;
            td3.innerHTML=user_content;
            td4.innerHTML=date.getFullYear() + '-' + (date.getMonth()+1) + '-' + date.getDay() + ' ' ;
            
        }
    </script>
</body>
</html>